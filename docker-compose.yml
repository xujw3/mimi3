services:
  mimi3:
    build:
      context: .
    image: mimi3:latest
    container_name: mimi3
    init: true
    restart: unless-stopped
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 512
    tmpfs:
      - /tmp
    env_file:
      - .env
    ports:
      - "${SERVER_PORT:-8000}:${SERVER_PORT:-8000}"
    volumes:
      - ./users:/app/users
      - ./logs:/app/logs
      - ./data:/app/data
